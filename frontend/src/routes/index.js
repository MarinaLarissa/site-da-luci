/**
 * Application Routes
 * Centralized route definitions for the application
 */

export const ROUTES = {
  LOOT_SPLIT: '/loot-split',
  SOLO_HUNT: '/solo-hunt',
  IMBUEMENT_CALC: '/imbuement-calc',
  BESTIARY_PLANNER: '/bestiary-planner',
};

/**
 * Default route (homepage)
 */
export const DEFAULT_ROUTE = ROUTES.LOOT_SPLIT;
